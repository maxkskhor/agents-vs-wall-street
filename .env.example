# Add only the variable names required by the final starter implementation.
# Never commit real API keys or tokens.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Optional overrides
ANTHROPIC_MODEL=
OPENAI_MODEL=
# Cheaper model used only for document fact extraction (default gpt-5.6-luna)
EXTRACT_MODEL=
